// import { createSlice } from '@reduxjs/toolkit';
// import { addTodoSuccess } from '../actions/authActions'; // Импорт экшена

// const todoSlice = createSlice({
//   name: 'todo',
//   initialState: { todos: [] }, // Начальное состояние - пустой массив
//   reducers: {}, // Пока нет локальных редюсеров
//   extraReducers: (builder) => {
//     builder.addCase(addTodoSuccess, (state, action) => {
//       state.todos.push(action.payload); // Добавление нового todo в массив
//     });
//   },
// });

// export default todoSlice.reducer;
